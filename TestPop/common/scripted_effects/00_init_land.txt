init_land = {
	every_province = {
		set_variable = { name = land_forest value = var:forest_score }
		change_variable = { name = land_forest multiply = var:prov_size }
		change_variable = { name = land_forest divide = 100 }
		
		set_variable = { name = land_cleared value = var:prov_size }
		change_variable = { name = land_cleared subtract = var:land_forest }
		
		set_variable = { name = land_farm value = var:farm_score }
		set_variable = { name = land_pasture value = var:pasture_score }
		change_variable = { name = land_farm multiply = var:land_farm }
		change_variable = { name = land_pasture multiply = var:land_pasture }
		
		set_variable = { name = divisor value = var:land_farm }
		change_variable = { name = divisor add = var:land_pasture }
		change_variable = { name = divisor add = 0.001 }
		change_variable = { name = land_farm divide = var:divisor }
		change_variable = { name = land_pasture divide = var:divisor }
		change_variable = { name = land_farm multiply = var:land_cleared }
		change_variable = { name = land_pasture multiply = var:land_cleared }
		change_variable = { name = land_farm multiply = var:farm_score }
		change_variable = { name = land_pasture multiply = var:pasture_score }
		
		change_variable = { name = land_forest multiply = 50 }
		change_variable = { name = land_farm multiply = 1.5 }
		change_variable = { name = land_pasture multiply = 1.5 }
		
		set_variable = { name = adder value = var:pop_total }
		change_variable = { name = adder multiply = 0.5 }
		change_variable = { name = land_farm add = var:adder }
		
		change_variable = { name = land_forest divide = 1000 }
		change_variable = { name = land_farm divide = 1000 }
		change_variable = { name = land_pasture divide = 1000 }
		
		change_variable = { name = land_forest multiply = 1000 }
		change_variable = { name = land_farm multiply = 1000 }
		change_variable = { name = land_pasture multiply = 1000 }
		
		change_variable = { name = land_forest add = 1 }
		change_variable = { name = land_farm add = 1 }
		change_variable = { name = land_pasture add = 1 }
		
		remove_variable = land_cleared
		remove_variable = divisor
		remove_variable = adder
	}
}